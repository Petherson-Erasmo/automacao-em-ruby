pipeline {
    agent {
        docker{
            image 'ruby'
        }
    }

    stages {
        stage('Preparation'){
            steps {
                sh 'bundle install'
            }
        }
        stage('Automatic testing') {
            steps {
                sh 'rspec'
            }
        }
    }
}
